spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:samapiece_dev}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

logging:
  level:
    sn.samapiece: DEBUG
